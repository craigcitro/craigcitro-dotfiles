
.First <- function() {
  options(repos = c(CRAN = "http://cran.fhcrc.org"),
          menu.graphics = FALSE,
          deparse.max.lines = 2,
          browserNLdisabled = TRUE)
}

if (interactive()) {
  suppressMessages(require(devtools))
}
