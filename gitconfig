[user]
        name = Craig Citro
        email = craigcitro@gmail.com
[alias]
        stat = status
        st = status
        br = branch
        ch = checkout
        substatus = submodule foreach "git status; true"
	l = "log -30 --abbrev-commit --relative-date --format='%Cred%h%Cgreen %aN%Cblue %ar %Creset %s'"
	lg = "log -60 --graph --color=always --abbrev-commit --date=relative --format='%Cred%h%Cgreen %aN%Cblue %ar %Creset %s'"
[color]
        ui = true
[core]
        excludesfile = ~/.gitignore
[grep]
        lineNumber = true
        
[push]
	default = tracking
